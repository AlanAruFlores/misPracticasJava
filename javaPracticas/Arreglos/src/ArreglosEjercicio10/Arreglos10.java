/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package ArreglosEjercicio10;
import javax.swing.JOptionPane;
import java.util.Scanner;
/**
 *
 * @author alang
 */
public class Arreglos10 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        /* Desarrollar un programa donde se declare un array de 10 elemento y pedir al usuario cuantas n posiciones se mueva el array
        Clase clss = new Clase();
        
        
        int[]arrayNumbers = new int[10];
        int[]numbers;
        int numberPosition, saveValue=9, valueArray=0;
        

        for(int i = 0;i<arrayNumbers.length; i++)
        {
            arrayNumbers[i] = Integer.parseInt(JOptionPane.showInputDialog("Ingrese el numero "+(i+1)));
        }
        
        clss.printArrayOriginal(arrayNumbers);
        
        numberPosition = Integer.parseInt(JOptionPane.showInputDialog("Ingrese a cuantas posiciones quiere que se muevan cada elemento"));
        numbers  = new int [numberPosition];
        valueArray = numberPosition;
        
        
        //Guadamos valores dentro de  array numbers
        for (int j = 0; j < numbers.length; j++)
        {
            numbers[j]=arrayNumbers[saveValue];
            saveValue--;
        }
        //Vamos moviendo los elementos de array
        for(int i = (9-numberPosition);i>=0; i--)
        {
            arrayNumbers[i+numberPosition] = arrayNumbers[i];
        }
        //Vamos ubicando los valores guardados en su posicion
        for(int i = 0 ; i<numbers.length;i++)
        {
            arrayNumbers[i] = numbers[valueArray-1];
            valueArray--;
        }
        
        
        //Imprimimos los valores guardados
        clss.printSaveValue(numbers);
        
        //Imprimimos los valores ya ordenados con su respectiva posicicion
        clss.printOrderValue(arrayNumbers);
     */

    }
}
